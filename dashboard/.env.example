# Copy to .env.local and fill in values. Never commit .env.local.

# Neon Postgres connection string (get from https://neon.tech)
DATABASE_URL=

# Groq API key — server-side only, never NEXT_PUBLIC_ (get from https://console.groq.com)
GROQ_API_KEY=
